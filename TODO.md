# TODO

- [ ] Update `app/layout.tsx` to set `lang` and `dir` based on selected language (RTL for Arabic).
- [ ] Update `components/sections/About.tsx` to use `useLanguage()` translations instead of hardcoded English.
- [ ] Update `components/sections/Services.tsx` to use `useLanguage()` translations instead of hardcoded English.
- [ ] Update `components/sections/Contact.tsx` to use `useLanguage()` translations instead of hardcoded English.
- [x] Update `app/portfolio/page.tsx` and `app/portfolio/[slug]/page.tsx` to use translations for headings/buttons.

- [ ] Ensure `messages/ar.json` contains required keys; if missing, add them (or align with existing keys).
- [ ] Run dev server and verify: language toggle flips RTL and all pages show Arabic text.

